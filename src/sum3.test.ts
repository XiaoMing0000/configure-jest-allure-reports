import { sum } from './sum';

describe('3333333333-1111111111111', () => {
	test('adds 1 + 2 to equal 3', () => {
		expect(sum(1, 2)).toBe(3);
	});

	test('3333333333-22222222222222', () => {
		expect(sum(2, 2)).toBe(3);
	});
});
