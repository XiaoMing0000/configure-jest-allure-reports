import { sum } from './sum';
import { join } from 'path';

describe('222222222-111111111111', () => {
	test('adds 1 + 2 to equal 3', () => {
		expect(sum(1, 2)).toBe(3);
	});

	test('222222222-2222222222222', () => {
		expect(sum(2, 2)).toBe(5);
	});
});
